const core = require('../ai-core');

module.exports = {
  resolveInteractiveAgentSetOptions: core.resolveInteractiveAgentSetOptions,
  runAgent: core.runAgent,
  runModelsList: core.runModelsList,
};
