# Validation Error Fixtures

This fixture matrix is the contract for production-readiness validation around Quiver lifecycle errors.

Each state in `matrix.json` represents a project condition that must stay covered by unit tests, CLI tests, or smoke tests before changing lifecycle behavior.
